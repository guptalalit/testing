# Onco-agent

To run use
streamlit run streamlit_app.py

if browser shows error, change 0.0.0.0 to localhost

Although requirements.txt contains all dependencies but in case of error let me know

All literature for Thyroid is part of repository


